# 《数据仓库与数据挖掘》期末大作业实验方案

> 对应题目：综合应用文本预处理、词向量、经典机器学习与深度学习分类算法，对同一数据集进行多方案对比实验，并撰写分析报告。

---

## 一、数据集选择与来源

### 1.1 数据集选型

| 项目 | 选择 | 说明 |
|------|------|------|
| 数据集名称 | **AG News（AG's News Topic Classification）** | NLP 文本分类论文中最常用的 benchmark 之一 |
| 任务类型 | 4 类别新闻主题分类 | World / Sports / Business / SciTech |
| 数据量 | **120,000** 条训练样本 + **7,600** 条测试样本 | 远超题目要求的 1 万条 |
| 属性个数 | 3 个：`label`(类别)、`title`(标题)、`description`(描述) | 将 `title + description` 拼接为 `text` |
| 类别平衡 | 完全平衡，每类 30,000 条训练样本、1,900 条测试样本 | 避免类别不平衡带来的偏差 |

### 1.2 数据来源（URL）

数据集由 Xiang Zhang 等人在 NIPS 2015 论文《Character-level Convolutional Networks for Text Classification》中构造，是公认的文本分类 benchmark。

- 数据集主页（含介绍、统计、引用）：
  https://paperswithcode.com/dataset/ag-news
- 原始 CSV 数据下载（PyTorch / CharCnn_Keras 维护，已被官方 torchtext 采用）：
  - 训练集：`https://raw.githubusercontent.com/mhjabreel/CharCnn_Keras/master/data/ag_news_csv/train.csv`
  - 测试集：`https://raw.githubusercontent.com/mhjabreel/CharCnn_Keras/master/data/ag_news_csv/test.csv`
- AG 语料原始介绍：
  http://www.di.unipi.it/~gulli/AG_corpus_of_news_articles.html
- tensorflow_datasets 页面（含版本、大小、字段说明）：
  https://www.tensorflow.org/datasets/catalog/ag_news_subset

### 1.3 数据格式示例

CSV 每行三列，标签用 `"` 转义、换行用 `\n` 转义：

```csv
"3","Wall St. Bears Claw Back Into the Black","Reuters - Short-sellers, Wall Street's dwindling band of ultra-cynics, are seeing green again."
"4","Oil and Economy Cloud Stocks' Outlook","NEW YORK (Reuters) - Soaring crude prices plus worries about the economy and the outlook for earnings are expected to hang over the stock market next week during the depth of the summer doldrums."
```

类别映射：`1-World / 2-Sports / 3-Business / 4-SciTech`。

### 1.4 属性业务含义

- `label`：监督学习的目标变量，新闻所属主题（4 类）。
- `title`：新闻标题，浓缩语义信息，词数短但信息密度高。
- `description`：新闻正文摘要，承担主要分类证据。
- 衍生 `text = title + " " + description`：作为统一输入文本。

---

## 二、实验总体目标

1. 在同一份数据上，**横向对比**三种文本向量化方式（词袋 / TF-IDF / Word2Vec）对分类效果的影响。
2. **纵向对比**经典机器学习算法与深度学习算法的分类效果。
3. 输出统一指标（Accuracy、Precision、Recall、F1、ROC-AUC）与 ROC 曲线，给出综合结论。

---

## 三、实验总体流程图

```
┌──────────────────────────┐
│ 1. 数据下载与加载 (AG News) │
└──────────────┬───────────┘
               │
┌──────────────▼───────────┐
│ 2. 数据清洗与中文/英文分词  │  （英文：nltk.word_tokenize + 停用词 + 小写）
└──────────────┬───────────┘
               │
       ┌───────┼───────┬────────────────┐
       │       │       │                │
┌──────▼─┐ ┌───▼───┐ ┌─▼──────────┐ ┌──▼─────────────┐
│ 词袋   │ │TF-IDF │ │ Word2Vec   │ │ BERT tokenizer │
│CountV  │ │TfidfV │ │gensim 自训 │ │ huggingface    │
└────┬───┘ └───┬───┘ └─┬──────────┘ └──┬─────────────┘
     │         │        │              │
┌────▼─────────▼────────▼──────────────▼──────────┐
│ 3. 切分：train / val / test  (8:1:1)            │
└──────────────────────┬──────────────────────────┘
                       │
       ┌───────────────┼────────────────┐
       │               │                │
┌──────▼──────┐  ┌─────▼─────┐  ┌───────▼─────────┐
│ LR / SVM    │  │ NaiveBayes│  │ BiLSTM / BERT   │
│ (2 经典算法) │  │ 或KNN/XGB │  │ (≥1 深度算法)   │
└──────┬──────┘  └─────┬─────┘  └───────┬─────────┘
       │              │                │
┌──────▼──────────────▼────────────────▼─────────┐
│ 4. 评估：Accuracy / Precision / Recall / F1 / ROC │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────┐
│ 5. 结果对比表 + ROC 曲线 + 分析报告             │
└─────────────────────────────────────────────────┘
```

---

## 四、实验步骤（每一步怎么做）

### 步骤 1：环境准备

**做法**：建立 Python 虚拟环境，安装依赖库。

```bash
python -m venv venv
pip install numpy pandas scikit-learn matplotlib seaborn
pip install nltk gensim torch torchvision
pip install transformers datasets
```

主要使用包及作用：

| 包 / 库 | 作用 |
|--------|------|
| `pandas` | 读取 CSV 数据集 |
| `nltk` | 英文分词、停用词、词形还原 |
| `sklearn.feature_extraction.text.CountVectorizer / TfidfVectorizer` | 词袋 / TF-IDF 向量化 |
| `gensim.models.Word2Vec` | 训练 Word2Vec 词向量 |
| `sklearn.linear_model.LogisticRegression` | 经典分类算法 1 |
| `sklearn.svm.LinearSVC` | 经典分类算法 2 |
| `torch` + `torch.nn.LSTM` | 深度学习 BiLSTM 分类器 |
| `transformers` (HuggingFace) | BERT 微调（加分项） |
| `sklearn.metrics` | Accuracy / Precision / Recall / F1 / ROC |

---

### 步骤 2：数据下载与加载

**做法**：用 `pandas.read_csv` 直接下载 GitHub 上的 train.csv 和 test.csv。

```python
import pandas as pd
train_url = "https://raw.githubusercontent.com/mhjabreel/CharCnn_Keras/master/data/ag_news_csv/train.csv"
test_url  = "https://raw.githubusercontent.com/mhjabreel/CharCnn_Keras/master/data/ag_news_csv/test.csv"
train_df = pd.read_csv(train_url, header=None, names=["label","title","description"])
test_df  = pd.read_csv(test_url,  header=None, names=["label","title","description"])
train_df["text"] = train_df["title"].astype(str) + " " + train_df["description"].astype(str)
test_df["text"]  = test_df["title"].astype(str)  + " " + test_df["description"].astype(str)
```

预期结果：训练集 120,000 条，测试集 7,600 条，共 4 类。

---

### 步骤 3：文本预处理

**做法**（英文文本）：

```python
import nltk, re
from nltk.corpus import stopwords
from nltk.stem import WordNetLemmatizer

nltk.download('punkt'); nltk.download('punkt_tab')
nltk.download('stopwords'); nltk.download('wordnet')

stop = set(stopwords.words('english'))
lemma = WordNetLemmatizer()

def preprocess(text):
    text = text.lower()
    text = re.sub(r"[^a-z\s]", " ", text)   # 去标点/数字
    tokens = nltk.word_tokenize(text)
    tokens = [lemma.lemmatize(w) for w in tokens if w not in stop and len(w) > 2]
    return " ".join(tokens)

train_df["clean"] = train_df["text"].apply(preprocess)
test_df["clean"]  = test_df["text"].apply(preprocess)
```

将原始 12 万条 + 7,600 条全部经过上述清洗，得到 `clean` 列。为节省时间，可先用 6 万条训练（每类 15,000）跑通，再扩展到全量。

---

### 步骤 4：数据集划分

**做法**：因官方已给出 test.csv，直接使用官方测试集作为最终评估；再从训练集中按 9:1 切出验证集用于调参。

```python
from sklearn.model_selection import train_test_split
X_train, X_val, y_train, y_val = train_test_split(
    train_df["clean"], train_df["label"],
    test_size=0.1, random_state=42, stratify=train_df["label"]
)
X_test, y_test = test_df["clean"], test_df["label"]
```

---

### 步骤 5：三种文本向量化方法

#### 5.1 词袋（Bag-of-Words，CountVectorizer）

```python
from sklearn.feature_extraction.text import CountVectorizer
bow = CountVectorizer(max_features=50000, ngram_range=(1,2), min_df=5)
X_tr_bow = bow.fit_transform(X_train)
X_va_bow = bow.transform(X_val)
X_te_bow = bow.transform(X_test)
```

#### 5.2 TF-IDF

```python
from sklearn.feature_extraction.text import TfidfVectorizer
tfidf = TfidfVectorizer(max_features=50000, ngram_range=(1,2), min_df=5, sublinear_tf=True)
X_tr_tfidf = tfidf.fit_transform(X_train)
X_va_tfidf = tfidf.transform(X_val)
X_te_tfidf = tfidf.transform(X_test)
```

#### 5.3 Word2Vec（必须用，可补充 GloVe/FastText）

**做法**：在训练语料上自训 Word2Vec 词向量，再对每个文本取平均得到文档向量（也可拼接 TF-IDF 加权平均）。

```python
from gensim.models import Word2Vec
sentences = [s.split() for s in train_df["clean"]]
w2v = Word2Vec(sentences, vector_size=100, window=5, min_count=5, workers=4, epochs=5)
w2v.save("w2v_agnews.bin")

import numpy as np
def doc_vector(tokens, model, dim=100):
    vecs = [model.wv[w] for w in tokens if w in model.wv]
    if not vecs: return np.zeros(dim)
    return np.mean(vecs, axis=0)

X_tr_w2v = np.vstack([doc_vector(s.split(), w2v) for s in X_train])
X_va_w2v = np.vstack([doc_vector(s.split(), w2v) for s in X_val])
X_te_w2v = np.vstack([doc_vector(s.split(), w2v) for s in X_test])
```

附加（加分）：可使用预训练 GloVe 6B 100d，URL 为 `https://nlp.stanford.edu/data/glove.6B.zip`。

---

### 步骤 6：经典机器学习分类算法（2 种）

#### 6.1 逻辑回归 Logistic Regression（覆盖题目要求 Bayesian 之外的线性模型，且评分标准明确提及 LR/SVM）

```python
from sklearn.linear_model import LogisticRegression
clf_lr = LogisticRegression(max_iter=1000, C=1.0, n_jobs=-1)
clf_lr.fit(X_tr_tfidf, y_train)         # 主要用 TF-IDF
pred_lr = clf_lr.predict(X_te_tfidf)
```

同时在 **词袋 / TF-IDF / Word2Vec** 三种向量化上各训练一次，做横向对比。

#### 6.2 线性 SVM（LinearSVC）

```python
from sklearn.svm import LinearSVC
clf_svm = LinearSVC(C=1.0)
clf_svm.fit(X_tr_tfidf, y_train)
pred_svm = clf_svm.predict(X_te_tfidf)
```

同样在三种向量化方式上各训练一次。

**附加（覆盖 Bayesian / KNN / XGBoost 等算法之一，加分项）**：

- Multinomial Naive Bayes：`sklearn.naive_bayes.MultinomialNB`，仅支持非负输入（适合词袋 / TF-IDF）。
- XGBoost：`xgboost.XGBClassifier`，适合 Word2Vec 文档向量。

---

### 步骤 7：深度学习分类算法（至少 1 种）

#### 7.1 BiLSTM（必做，覆盖 RNN/LSTM/GRU）

**做法**：将分词后的文本映射为 Word2Vec 词索引序列，送入双向 LSTM。

```python
import torch, torch.nn as nn
from torch.nn.utils.rnn import pad_sequence

# 词表与词向量矩阵
vocab = {w: i+2 for i, w in enumerate(w2v.wv.index_to_key)}  # 0=pad, 1=unk
def encode(tokens, max_len=200):
    ids = [vocab.get(w, 1) for w in tokens][:max_len]
    return ids

MAX_LEN = 200
X_tr_ids = [torch.tensor(encode(s.split())) for s in X_train]
X_tr_ids = pad_sequence(X_tr_ids, batch_first=True, padding_value=0).[:, :MAX_LEN]

class BiLSTMClassifier(nn.Module):
    def __init__(self, embedding_matrix, hidden=128, num_classes=4):
        super().__init__()
        num_emb, dim = embedding_matrix.shape
        self.embedding = nn.Embedding.from_pretrained(
            torch.tensor(embedding_matrix), padding_idx=0, freeze=False)
        self.lstm = nn.LSTM(dim, hidden, batch_first=True, bidirectional=True)
        self.fc = nn.Linear(2*hidden, num_classes)

    def forward(self, x):
        x = self.embedding(x)
        _, (h, _) = self.lstm(x)
        h = torch.cat([h[-2], h[-1]], dim=1)
        return self.fc(h)
```

**模型结构参数**：

| 项 | 取值 |
|------|------|
| Embedding 维度 | 100（与 Word2Vec 一致） |
| 序列最大长度 | 200 |
| LSTM 隐藏层 | 128（双向 → 拼接 256） |
| 优化器 | Adam，lr=1e-3 |
| Batch size | 64 |
| Epoch | 3~5 |
| 训练 / 验证 / 测试 | 训练集 90% / 训练集 10% / 官方测试集 |

#### 7.2 BERT 微调（加分项，覆盖 BERT）

使用 HuggingFace 的预训练 `bert-base-uncased`（URL：`https://huggingface.co/bert-base-uncased`），在 AG News 上 Fine-tune 2~3 个 epoch，输入采用 BERT 自带的 WordPiece tokenizer。

---

### 步骤 8：评估指标与 ROC 曲线

**做法**：对每一组 (向量化方式 × 分类算法) 计算指标，并保存 ROC 曲线。

```python
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, roc_auc_score, roc_curve
import matplotlib.pyplot as plt
from sklearn.preprocessing import label_binarize

def evaluate(y_true, y_pred, y_score=None, n_classes=4):
    acc  = accuracy_score(y_true, y_pred)
    prec = precision_score(y_true, y_pred, average="macro")
    rec  = recall_score(y_true, y_pred, average="macro")
    f1   = f1_score(y_true, y_pred, average="macro")
    auc  = roc_auc_score(label_binarize(y_true, classes=range(n_classes)),
                         y_score, average="macro", multi_class="ovr") if y_score is not None else None
    return {"acc":acc, "prec":prec, "rec":rec, "f1":f1, "auc":auc}

# 绘制多分类 ROC（one-vs-rest macro-average）
y_test_bin = label_binarize(y_test, classes=range(4))
fpr, tpr, _ = roc_curve(y_test_bin.ravel(), y_score.ravel())
plt.plot(fpr, tpr, label=f"BiLSTM (AUC={auc:.3f})")
plt.plot([0,1],[0,1],"k--")
plt.xlabel("FPR"); plt.ylabel("TPR"); plt.legend()
plt.savefig("roc_bilstm.png", dpi=150)
```

---

### 步骤 9：结果汇总与对比

**做法**：把所有实验结果汇总到一个 `results.csv` 或对比表。

| 向量化 | 分类器 | Accuracy | Precision | Recall | F1 | AUC |
|--------|--------|----------|-----------|--------|----|----|
| 词袋 | LR | … | … | … | … | … |
| 词袋 | SVM | … | … | … | … | … |
| TF-IDF | LR | … | … | … | … | … |
| TF-IDF | SVM | … | … | … | … | … |
| Word2Vec | LR | … | … | … | … | … |
| Word2Vec | SVM | … | … | … | … | … |
| Word2Vec | BiLSTM | … | … | … | … | … |
| BERT | BertClassifier | … | … | … | … | … |

并把每个模型的 ROC 曲线画在同一张图上对比（5 分加分）。

---

### 步骤 10：结果分析与报告撰写

**做法**：根据上表回答以下问题，填入报告：

1. **同一分类算法、不同预处理方法的差异**：例如 LR 在词袋 vs TF-IDF vs Word2Vec 上的差距。重点分析 TF-IDF 抑制高频词、Word2Vec 引入语义但维度低的特点。
2. **同一预处理、不同分类算法的差异**：例如 TF-IDF 下 LR / SVM / NB 的对比。
3. **经典 ML vs 深度学习**：BiLSTM / BERT 通常比 LR/SVM 高 1~3 个百分点，但训练成本高、所需数据量大。
4. **指标解释**：在报告「分析结果」一节中说明 Accuracy / Precision / Recall / F1 的含义（10 分项）。

---

## 五、目录结构与代码文件清单

```
源代码/
├── 实验方案.md                      # 本文件
├── 报告.pdf / 报告.docx              # 期末大作业报告
├── data/                            # 数据目录（运行后自动生成）
│   ├── train.csv
│   ├── test.csv
│   └── agnews_clean.csv
├── models/                          # 训练好的模型与词向量
│   ├── w2v_agnews.bin
│   └── bilstm_best.pt
├── figures/                         # 结果图
│   ├── roc_*.png
│   └── metrics_compare.png
├── results/
│   └── results.csv                  # 所有实验结果汇总
└── src/
    ├── 1_data_prepare.py            # 下载 + 清洗 + 划分
    ├── 2_vectorize_bow_tfidf.py     # 词袋 / TF-IDF 向量化
    ├── 3_train_word2vec.py          # 训练 Word2Vec
    ├── 4_classic_lr.py              # LR + 词袋/TF-IDF/Word2Vec
    ├── 4_classic_svm.py             # SVM + 词袋/TF-IDF/Word2Vec
    ├── 4_classic_nb.py              # NB（加分）
    ├── 4_classic_xgb.py             # XGBoost（加分）
    ├── 5_bilstm.py                  # BiLSTM 分类器
    ├── 6_bert.py                    # BERT 分类器（加分）
    └── 7_evaluate.py                # 统一评估与 ROC 绘图
```

运行顺序：`1 → 2 → 3 → 4_* → 5 → 6(可选) → 7`。

---

## 六、复现命令（一键复跑）

```bash
# 1) 数据与预处理
python src/1_data_prepare.py

# 2) 三种向量化
python src/2_vectorize_bow_tfidf.py
python src/3_train_word2vec.py

# 3) 经典机器学习
python src/4_classic_lr.py
python src/4_classic_svm.py
python src/4_classic_nb.py
python src/4_classic_xgb.py

# 4) 深度学习
python src/5_bilstm.py
python src/6_bert.py

# 5) 评估与可视化
python src/7_evaluate.py
```

---

## 七、预期成果

- 训练日志、模型权重（`models/`）。
- 指标对比表（`results/results.csv`）。
- 9 张左右 ROC 曲线图（`figures/`）。
- 一份完整的中文实验报告，覆盖：
  1. 数据来源与说明（含 URL）— 10 分；
  2. 三种向量化方法原理与实现 — 15 分；
  3. 经典 ML × 深度学习 × 多种预处理对比 — 50 分；
  4. Accuracy / Precision / Recall / F1 解释 + 表格 + ROC 曲线 — 25 分。

---

> 备注：若因设备性能原因无法跑完 12 万条数据，方案可缩到 3~6 万条，仍可保证「1 万条以上」的硬性要求；推荐先用 6 万条跑通全流程，再视时间扩展到 12 万。