# AG News 文本分类实验——各脚本说明

> 本文档说明从 `1_data_prepare.py` 到 `7_evaluate.py` 共 7 个脚本的功能、输入输出、技术细节及运行顺序。

---

## 脚本总览

| 脚本 | 功能 | 输出文件 |
|------|------|---------|
| `1_data_prepare.py` | 下载原始数据 → 文本预处理与清洗 | `data/agnews_clean_train.csv`、`data/agnews_clean_test.csv` |
| `2_vectorize_bow_tfidf.py` | 词袋与 TF-IDF 向量化 | `models/bow_*.npz`、`models/tfidf_*.npz` |
| `3_train_word2vec.py` | 在训练语料上训练 Word2Vec 词向量 | `models/w2v_agnews.model` |
| `4_classic_lr.py` | 逻辑回归分类器（3 种向量化） | `models/lr_*.pkl`、`results/lr_results.csv` |
| `4_classic_svm.py` | 线性 SVM 分类器（3 种向量化） | `models/svm_*.pkl`、`results/svm_results.csv` |
| `5_bilstm.py` | BiLSTM 深度学习分类器 | `models/bilstm_best.pt`、`results/bilstm_results.csv` |
| `7_evaluate.py` | 统一评估与多维可视化 | `results/all_results.csv`、`figures/*.png`（6 张图） |

**运行顺序**：`1 → 2 → 3 → 4_lr → 4_svm → 5 → 7`

---

## 脚本详解

---

### 1_data_prepare.py（数据下载与预处理）

**目的**：从网络下载 AG News 原始 CSV 数据集，并完成文本清洗（分词、去停用词、词形还原）。

**输入**：
- 网络：两个 CSV 的 GitHub 原始地址
- 本地（若已存在）：`data/train.csv`、`data/test.csv`

**处理流程**：

```
原始 CSV（label, title, description）
    ↓
合并 title + description → text
    ↓
preprocess(text):
    1. 小写化   lower()
    2. 去除标点/数字   re.sub(r"[^a-z\s]", " ", text)
    3. 分词   nltk.word_tokenize()
    4. 去停用词 + 词长>2
    5. 词形还原   WordNetLemmatizer()
    ↓
输出 CSV（label, clean）
```

**关键技术细节**：
- CSV 无表头，需指定 `header=None, names=["label", "title", "description"]`
- NLTK 语料库（`punkt`、`punkt_tab`、`wordnet`、`stopwords`）存放于项目内 `nltk_data/`，通过 `nltk.data.path.insert(0, ...)` 优先使用本地资源，避免依赖系统路径
- 原始 `punkt_tab.zip` / `wordnet` 数据结构不完整，通过 Python `zipfile` 解压并合并到正确目录结构

**输出**：
- `data/agnews_clean_train.csv`：120,000 行
- `data/agnews_clean_test.csv`：7,600 行

**已知修复**：首次运行需确保 `nltk_data/tokenizers/punkt_tab/` 下有 `english/` 子目录（含 `collocations.tab`），以及 `nltk_data/corpora/wordnet/` 下有 `lexnames` 等文件，否则会报 `FileNotFoundError`。

---

### 2_vectorize_bow_tfidf.py（BOW 与 TF-IDF 向量化）

**目的**：将清洗后的文本转换为可用于传统机器学习的数值向量表示。

**输入**：`data/agnews_clean_train.csv`、`data/agnews_clean_test.csv`

**向量化方法**：

| 方法 | 类别 | 参数 |
|------|------|------|
| **CountVectorizer**（词袋） | 离散频次 | `max_features=50000`，`ngram_range=(1,2)`，`min_df=5` |
| **TfidfVectorizer**（TF-IDF） | 逆文档频率加权 | `max_features=50000`，`ngram_range=(1,2)`，`min_df=5`，`sublinear_tf=True` |

**输出**（均以稀疏矩阵格式保存，节省空间）：
- `models/bow_train.npz` / `models/bow_test.npz`
- `models/tfidf_train.npz` / `models/tfidf_test.npz`
- `models/bow_vectorizer.pkl` / `models/tfidf_vectorizer.pkl`（含词汇表，可对新文本 transform）
- `models/y_train.npy` / `models/y_test.npy`（标签数组）

---

### 3_train_word2vec.py（训练 Word2Vec 词向量）

**目的**：在训练语料上自训练词向量，捕获词语间的语义相似性。

**输入**：`data/agnews_clean_train.csv`（120,000 条分词文本）

**模型参数**：

| 参数 | 值 | 说明 |
|------|----|------|
| `vector_size` | 100 | 词向量维度 |
| `window` | 5 | 上下文窗口大小 |
| `min_count` | 5 | 词频阈值，低于此则丢弃 |
| `workers` | 4 | 并行训练线程数 |
| `epochs` | 10 | 训练轮数 |
| `seed` | 42 | 随机种子，保证可复现 |

**输出**：
- `models/w2v_agnews.model`：`gensim.Word2Vec` 模型文件，可直接加载
- `models/w2v_agnews.vectors.npy`：纯向量矩阵（不含词汇表，便于直接使用）

---

### 4_classic_lr.py（逻辑回归分类器）

**目的**：用逻辑回归在 3 种向量化表示上分别训练，评估不同向量化方式对线性分类器的影响。

**输入**：
- `models/bow_*.npz`、`models/tfidf_*.npz`（稀疏矩阵）
- `models/w2v_agnews.model`（词向量，需额外构建文档向量）
- `models/y_train.npy` / `models/y_test.npy`

**训练 3 次**：

| 组合 | 向量化维度 | 特点 |
|------|-----------|------|
| LR + BOW | 50,000 维稀疏 | 保留词频信息，无权重衰减 |
| LR + TF-IDF | 50,000 维稀疏 | 抑制高频词，突出区分性词汇 |
| LR + Word2Vec | 100 维稠密 | 捕获语义相似性，维度低 |

**文档向量构建方式**（Word2Vec 专用）：
- 对文本分词，查找词向量表中每个词的向量
- 取所有词向量的均值作为文档向量；若文本为空则用零向量

**评估指标**：Accuracy、Precision (macro)、Recall (macro)、F1 (macro)、ROC-AUC (macro, ovr)

**已知修复**：
- `label_binarize` 在新版 scikit-learn 中已移除，改为使用 `LabelBinarizer` 类
- 结果 CSV 中自动补全 `model` 列（如 "LR"），与 `7_evaluate.py` 合并时保持一致

**输出**：`models/lr_bow.pkl`、`models/lr_tfidf.pkl`、`models/lr_w2v.pkl`、`results/lr_results.csv`

---

### 4_classic_svm.py（线性 SVM 分类器）

**目的**：用线性支持向量机在 3 种向量化表示上分别训练，与逻辑回归形成横向对比。

**输入**：与 `4_classic_lr.py` 完全相同。

**训练 3 次**：SVM + BOW、SVM + TF-IDF、SVM + Word2Vec

**关键技术细节**：
- `LinearSVC` 本身没有 `predict_proba`，使用 `CalibratedClassifierCV` 包装（`cv=3`）获得概率输出，以便计算 ROC-AUC
- 标签同样减 1 处理（原始 1-4 → 模型输入 0-3）

**已知修复**：与 LR 脚本相同，`label_binarize` → `LabelBinarizer`。

**输出**：`models/svm_bow.pkl`、`models/svm_tfidf.pkl`、`models/svm_w2v.pkl`、`results/svm_results.csv`

---

### 5_bilstm.py（BiLSTM 深度学习分类器）

**目的**：使用双向 LSTM 深度学习模型，在 Word2Vec 词向量基础上进行端到端文本分类。

**输入**：
- `data/agnews_clean_train.csv`、`data/agnews_clean_test.csv`（原始文本）
- `models/w2v_agnews.model`（词向量矩阵）
- 训练集 90% / 10% 切分（验证集）

**模型结构**：

```
输入文本  →  分词  →  词索引  →  Embedding(词向量)  →  BiLSTM  →  拼接隐藏状态  →  Dropout  →  FC(Softmax)
                      ↑
              词表: <pad>=0, <unk>=1, 2..N=word
```

| 组件 | 参数 |
|------|------|
| Embedding | 100 维（来自 Word2Vec），`padding_idx=0`，可微调 |
| LSTM | 128 隐藏单元，双向（2×128=256 拼接） |
| Dropout | 0.3（仅在全连接层，移除了 LSTM 内部无效 dropout） |
| 优化器 | Adam，`lr=1e-3` |
| Batch size | 64 |
| Epoch | 6 |
| 序列最大长度 | 200 |

**训练策略**：
- 每轮在验证集上评估，保存验证准确率最高的模型权重到 `models/bilstm_best.pt`
- 评估时用最优权重重新加载

**已知修复**：
- 原始标签 1-4（AG News 原始标注），PyTorch `CrossEntropyLoss` 要求 0-3，在 `TextDataset` 中统一减 1

**输出**：`models/bilstm_best.pt`、打印最终测试集结果（含 Acc/Prec/Rec/F1/AUC）、`results/bilstm_results.csv`

---

### 7_evaluate.py（统一评估与多维可视化）

**目的**：汇总所有实验结果，输出对比表 + 6 张多维可视化图表（全部中文标注）。

**输入**：`results/lr_results.csv`、`results/svm_results.csv`、`results/bilstm_results.csv`

**6 张可视化图表**：

| 编号 | 图表类型 | 说明 |
|------|---------|------|
| 1 | **柱状对比图** | 所有模型 × 5 个指标，分组柱状图 |
| 2 | **雷达图** | 每条曲线代表一个模型，5 轴展示综合能力 |
| 3 | **环形图** | 各模型 Accuracy 在所有模型中的占比分布 |
| 4 | **饼图** | 各模型 F1 得分分布，含分离效果 |
| 5 | **折线图** | 横轴为向量化方法（BOW/TF-IDF/Word2Vec），3 条折线分别代表 BiLSTM/LR/SVM，5 个子图分别对应 5 个指标 |
| 6 | **热力图矩阵** | 6 张独立热力图，展示模型×向量化方法在每个指标下的数值，颜色映射表示性能高低 |

**字体处理**：自动检测系统可用中文字体（Microsoft YaHei / SimHei / STKaiti 等），确保所有标签显示正常。

**输出**：
- `results/all_results.csv`：所有模型完整对比表
- `figures/metrics_compare.png`（柱状图）
- `figures/metrics_radar.png`（雷达图）
- `figures/metrics_donut.png`（环形图）
- `figures/metrics_pie.png`（饼图）
- `figures/metrics_line.png`（折线图）
- `figures/metrics_heatmap.png`（热力图）

---

## 最终实验结果

| 模型 | 向量化 | Accuracy | Precision | Recall | F1 | AUC |
|------|--------|----------|-----------|--------|----|----|
| BiLSTM | Word2Vec | **0.9209** | 0.9217 | 0.9209 | 0.9207 | **0.9872** |
| SVM | TF-IDF | 0.9199 | 0.9198 | 0.9199 | 0.9198 | 0.9843 |
| LR | TF-IDF | 0.9143 | 0.9141 | 0.9143 | 0.9142 | 0.9834 |
| SVM | BOW | 0.9087 | 0.9085 | 0.9087 | 0.9085 | 0.9814 |
| LR | BOW | 0.9071 | 0.9070 | 0.9071 | 0.9070 | 0.9831 |
| SVM | Word2Vec | 0.8859 | 0.8854 | 0.8859 | 0.8856 | 0.9720 |
| LR | Word2Vec | 0.8830 | 0.8827 | 0.8830 | 0.8828 | 0.9728 |

**各指标最优组合**：
- 准确率 (Acc)：BiLSTM + Word2Vec，92.09%
- 精确率 (Prec)：BiLSTM + Word2Vec，92.17%
- 召回率 (Rec)：BiLSTM + Word2Vec，92.09%
- F1 值 (F1)：BiLSTM + Word2Vec，92.07%
- AUC 值 (AUC)：BiLSTM + Word2Vec，98.72%

---

## 一键运行命令

```bash
# 按顺序执行以下命令即可复现全部实验
python src/1_data_prepare.py          # 数据下载与预处理
python src/2_vectorize_bow_tfidf.py  # BOW / TF-IDF 向量化
python src/3_train_word2vec.py        # Word2Vec 训练
python src/4_classic_lr.py            # 逻辑回归
python src/4_classic_svm.py            # 线性 SVM
python src/5_bilstm.py                 # BiLSTM（CPU 约需 1 小时）
python src/7_evaluate.py               # 评估与可视化
```
